# Vagrantfile for testing the box

Vagrant.configure(2) do |config|
  config.vm.box = "debian83"
  config.vm.box_url = "https://github.com/korekontrol/packer-debian8/releases/download/ci-8/debian83.box"
end
